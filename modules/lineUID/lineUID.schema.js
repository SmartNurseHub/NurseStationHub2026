/*****************************************************************
 * LINE UID SCHEMA / CONFIG MODULE
 * NurseStationHub
 *
 * ---------------------------------------------------------------
 * หน้าที่:
 * - กำหนดชื่อ Google Sheet สำหรับ LineUID
 * - รองรับการตั้งค่าผ่าน environment (.env)
 *
 * ---------------------------------------------------------------
 * CONFIG PRIORITY:
 *
 * 1. process.env.SHEET_LineUID   (จาก environment)
 * 2. "LineUID"                  (default fallback)
 *
 * ---------------------------------------------------------------
 * การใช้งาน:
 * const { LINE_UID_SHEET } = require(...)
 *
 * ---------------------------------------------------------------
 * หมายเหตุ:
 * - ใช้ fallback เพื่อป้องกันระบบล่มกรณีไม่ได้ตั้ง .env
 *****************************************************************/


/* =========================================================
   SHEET CONFIG
========================================================= */

const LINE_UID_SHEET = process.env.SHEET_LineUID || "LineUID";


/* =========================================================
   EXPORT
========================================================= */

module.exports = {
  LINE_UID_SHEET
};